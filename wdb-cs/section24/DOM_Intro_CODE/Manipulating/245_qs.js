const allImg = document.querySelector('a');

console.log('The total number of links are ',allImg);

const imgs = document.querySelectorAll('.square');

console.log('The number of images having square class is ',imgs.length);